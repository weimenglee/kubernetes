apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    name: nginx
  namespace: test1
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
      - containerPort: 80
    imagePullPolicy: Always
